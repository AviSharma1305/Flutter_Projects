import 'package:flutter/material.dart';

const kprime2 = Color(0xFFFFC61F);
const ksecond = Color(0xFF86242A);
const TextCol = Color(0xFF50505D);
const CombColr = Color(0xFFCF3476);
final kprime = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [
    Color(0xFF86242a).withOpacity(0.75),
    Colors.redAccent.withOpacity(0.75),
  ],
);